import client from './client'

// Future — fil d'activité des matches (tutos, posts, annonces de sessions)
// export const getFeed        = ()           => client.get('/api/feed')
// export const getProfileFeed = (userId)     => client.get(`/api/feed/user/${userId}`)
// export const createPost     = (data)       => client.post('/api/feed', data)
// export const deletePost     = (postId)     => client.delete(`/api/feed/${postId}`)
